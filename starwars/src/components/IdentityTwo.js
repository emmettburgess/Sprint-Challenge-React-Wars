import React from 'react';

const IdentityTwo = props => {
    return (
        <p>
            <strong>Identity:</strong> {props.character.identity}
        </p>
    );
};

export default IdentityTwo;